using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;

public class SpawnScript : MonoBehaviour
{
    //int NumberOfPellets = 28;
    //public float offset = 0.299f;
    //public float currentPellet;
    //// @Start is called before the first frame update
    //void Start()
    //{
    //    // @Name all the game Objects Node

    //    //gameObject.name = "Node";
    //    //return;

    //    if(gameObject.name == "Node")
    //    {
    //        currentPellet = offset;
            //  for(int i=0, i < NumberOfPellets, int++)
            //{

    //        // @Clone new nodes along X axis

    //        Instantiate(gameObject,new Vector3(transform.position.x + currentPellet, transform.position.y,0 ), Quaternion.identity);
    //        currentPellet += offset;

    //        // @Clone new nodes along Y axis (add currentPellet to Y transform and remove from X transform
    //       }
    //    }

    //}

    //// Update is called once per frame
    //void Update()
    //{

    //}
}
